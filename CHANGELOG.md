# 更新日志

这里记的是我每次改了什么、为什么改、踩了什么坑。不是 changelog 模板,是给自己留的迭代笔记。

---

## v1.13 (build 16) —— 2026-09-16

### 我修了什么

**发布包"文件已损坏"** —— 有人下载后打不开,右键打开、隐私与安全性全都没用。查下来是我自己的锅。

### 我踩的坑

`codesign -vv --strict` 报了这么一句:

```
a sealed resource is missing or invalid
file missing: Contents/MacOS/LaunchPad.cstemp
```

翻译过来:签名清单里封了一个根本不存在的文件。

原因是我用 `codesign --force --deep` 反复重签,但**签名前没清掉上一轮的 `_CodeSignature`**。
codesign 会把包内文件清单写进 `CodeResources`,旧清单还在,它就把上次签名留下的临时文件
`LaunchPad.cstemp` 当成"包内资源"一起封进了新签名。签名从此无效,macOS 对签名损坏的 app
一律判"文件已损坏",不给你任何绕过的机会。

本机一直没暴露,是因为本地装的 app 没有隔离标记,Gatekeeper 压根不查它。只有别人下载才会中招。

### 改法

1. 签名前 `rm -rf "$APP/Contents/_CodeSignature"` 并清掉 `*.cstemp`
2. 去掉 `--deep`(包内没有嵌套的 framework,不需要它)
3. build.sh 加了**签名自检**:`codesign -vv --strict` 不过就直接构建失败

### 现在的效果

- `codesign -vv` → valid on disk ✅
- `spctl` → rejected(ad-hoc 签名没有苹果开发者证书,这个改不掉,除非交 99 美元/年)
- 用户那边从"**文件已损坏**"(死路一条)变成"**来自身份不明的开发者**"(右键→打开 就能用)

---

## v1.14 (build 17) —— 2026-09-16

### 起因

有人用着我发的 LaunchPad,问我背景为什么跟桌面壁纸不是一个东西。
我一开始以为是取错了屏,查下来发现是三个坑叠在一起,其中一个我 v1.13 还写错了结论。

### 我修了什么

**1. 背景永远锁在同一张旧照片上(自我污染)**

照片池靠"谁的访问时间最新"来猜当前壁纸。而我读图那一下,会把选中那张的
atime 刷成"现在" —— 下次启动它还是最新,再选它,再刷……**永久自锁**。
选对了就一直对,选错了就一直错,怎么换壁纸都不变。

改法:读图之前记下 atime,读完用 `utimensat` 恢复回去(Darwin 的 `UTIME_OMIT` 是 -2,
只改 atime 不动 mtime)。这是用户自己的文件,不需要任何权限。

**2. 老版本已经污染过的文件,做一次善后**

被旧版本污染过的那张,atime 已经是启动时刻,我分不清它是"被污染"还是"真的刚用过"。
所以在 v1.14 首次启动时做一次:把当前 atime 最新的那张拉回它的 mtime,
让池子重新按系统时间排序。做完一次就打标记,不再重复。

**3. 同秒并列时背景会随机变**

系统预载下一张时会一次读好几张,atime 完全相同。老写法用 `max(by:)`,
选谁取决于目录遍历顺序。改成 (atime 降序, 文件名升序) 排序,结果确定。

- **顺带抓到"文件已损坏"的复发**:打包前随手校验,发现刚签好的 app 又坏了 ——
  `Contents/MacOS` 里冒出一个 `LaunchPad.cstemp`。这是 codesign 自己写临时文件
  再改名留下的残骸,**签名时它还不存在**,所以只在签名前清理根本管不到它,
  自检那会儿也确实显示 valid,等下次校验才暴露。
  改法:签名前清一次,**签完再清一次**。别省后面那一步,不然用户下载又是"已损坏"。

### 顺手改的两处

- **右上角"n 个"数量删掉了** —— 我自己看了一眼也觉得没用,占地方而已。
- **新增背景模糊度调节条**(右上角滑杆图标):macOS 27 系统设置那种深色胶囊
  面板,左端空心方块 = 模糊弱,右端实心方块 = 模糊强,拖动实时改背景。
  范围 0~40(0 就是完全清晰),默认值还是原来的 16,存在 UserDefaults 里,下次打开还在。

### 我查清但没修的

- **Apple 压根没记录"当前显示哪一张"**。Index.plist 里照片池那种 Choice 的 `Files`
  是空的,EncodedOptionValues、连壁纸视频的 UUID 都搜不到。
  v1.13 我在注释里写"解析 plist 能彻底准确" —— **这个判断是错的,已经改掉**。
- **航拍视频壁纸**。桌面壁纸可以是 Apple 的 4K 航拍视频(比如"金门大桥·日落"),
  这时 `desktopImageURL` 返回的是漂移的上级目录,我们照旧下探到照片池,
  挑出来的必然是错的 —— 池子里可能根本没有当前壁纸。
  我自己的机器就是这个情况,所以**这个版本我的背景依然跟桌面不一致**,
  要修得靠手动指定背景图或视频选帧,那是另一个工作量,先不动。
- **relatime 挂载**:这块盘不是每次渲染都刷 atime,atime 只能当粗略信号。修不了。

---

## v1.12 (build 15) —— 2026-09-15

开源首发版。

- 文件夹里的 app 可以拖出来了,拖动时会显示"松手:移回主页"的提示条
- 文件夹内部可以拖动排序了
- 删掉了没用的"编辑"按钮,想删文件夹改成右键菜单
- 滚动性能优化:主线程 ≥25ms 的卡顿从 2.65% 降到 1.25%,实际掉帧从 3.9% 降到 1.95%
- 文件夹图标从 2×2 换成 3×3

---

**创作者-星之所向-阿星陪你走过丝绸之路**
一个骑行的人,顺手写的 Mac 小工具。
